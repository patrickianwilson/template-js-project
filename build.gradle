if (rootProject.hasProperty("buildNum")) {
    version = rootProject.getProperties().get("buildNum")
}

apply from: "$rootProject.projectDir/config/gradle/js/js-conventions.gradle"
project.ext {
    dockerTag = "welcome:latest"
    dockerFile = "$projectDir/deploy/Dockerfile"
    dockerWorkingDir = "$projectDir/deploy"

    serviceYamlFileTemplate = "$projectDir/deploy/service.yaml"

    dockerImagePath = "us.gcr.io/inlaid-citron-94802/codetoolbox/node:${project.version}"
}


//must set the ext property first.
apply from: "$rootProject.projectDir/config/gradle/js/docker.gradle"
apply from: "${rootProject.projectDir}/config/gradle/kubernetes.gradle"
